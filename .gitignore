# Yerel veri seed'i / DB dump'ı — asla repoya push edilmez (özel veri içerir)
/seed/

# KURAL: Excel verileri HİÇBİR ŞEKİLDE GitHub'a push edilmez (özel veri içerir).
# Bu, doldurulmuş "Veri Çek" şablonları dahil tüm .xlsx/.xls dosyalarını kapsar.
# Boş şablonlar gerektiğinde frontend/prisma/generate-templates.ts ile üretilir.
*.xlsx
*.xls
*.xlsm
*.dump
*.sql
*.csv

# Yerel Docker mount yapılandırması (makineye özel yollar) — push edilmez
docker-compose.override.yml

# Secret'lar (JWT_SECRET, DB şifresi vb.) — asla repoya push edilmez
.env
